{
  "buildCommand": "npm run build",
  "outputDirectory": "build/client",
  "framework": null,
  "serverFiles": ["build/server/**"],
  "regions": ["iad1"],
  "env": {
    "NODE_ENV": "production"
  }
}

